@page "/study"
@using TCSA.KnowTheCity.Core.Helpers

<MudContainer>
    <MudText Typo="Typo.body1" Class="mb-4">Select a city to study its landmarks!</MudText>

    <MudGrid>
        @foreach (var city in CityDataHelper.Cities)
        {
            <MudItem xs="6" sm="4" md="3">
                <MudCard Elevation="3"
                         Class="city-card"
                         Style="cursor:pointer;"
                         @onclick="() => NavigateToStudy(city.Name)">
                    <MudCardMedia Image="@CityDataHelper.GetCityImagePath(city.Name)" Height="200" />
                    <MudCardContent Class="d-flex justify-center">
                        <MudText Typo="Typo.h6">@city.Name</MudText>
                    </MudCardContent>
                </MudCard>
            </MudItem>
        }
    </MudGrid>
</MudContainer>

@code {
    [Inject]
    private NavigationManager Navigation { get; set; } = default!;

    private void NavigateToStudy(string cityName) => Navigation.NavigateTo($"study/{cityName}");
}